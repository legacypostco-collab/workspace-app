Django==5.2.16
djangorestframework==3.15.2
drf-spectacular==0.29.0
reportlab==4.2.2
pypdf==6.14.2
Pillow==12.3.0
openpyxl==3.1.5
XlsxWriter==3.2.0
defusedxml==0.7.1

# Database
dj-database-url==3.1.2
psycopg2-binary==2.9.12

# Static files
whitenoise==6.7.0

# Background tasks
celery==5.4.0
redis==5.0.8
django-celery-beat==2.9.0
django-celery-results==2.6.0

# WebSocket
channels==4.3.2
channels-redis==4.3.0
daphne==4.2.2

# 2FA / TOTP
pyotp==2.9.0
qrcode==8.2
cryptography>=43.0.0,<50.0.0

# Production server
gunicorn==23.0.0

# Optional: Sentry error tracking
sentry-sdk==2.16.0

# AI Assistant
anthropic==0.97.0
pgvector==0.4.2
voyageai==0.2.4
httpx==0.28.1

# Payments — Stripe (опц., если нет STRIPE_SECRET_KEY — используется встроенная Wallet-эскроу)
stripe==15.1.0

# E2E testing (only needed in dev/CI; ставится отдельно с `playwright install chromium`)
# playwright>=1.50.0
# pytest-playwright>=0.5

# ── Production hardening (опционально, активируются при наличии) ──
django-axes==7.0.0          # Brute-force protection (settings.py: auto-enabled if installed)
django-storages==1.14.4     # S3/MinIO/Yandex Object Storage (требует boto3)
boto3==1.35.0               # AWS SDK для django-storages
django-csp==4.0             # Content Security Policy middleware
clamd==1.0.2                # ClamAV daemon client для скана загрузок
coverage==7.6.10            # Test coverage в CI
